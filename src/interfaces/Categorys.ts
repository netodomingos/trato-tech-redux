
export interface Categorys {
    nome: string,
    thumbnail: string,
    header: string,
    id: string,
    descricao: string,
}

export interface CategoryComponent {
    titulo: string,
    descricao: string,
    foto: string,
    favorito: boolean,
    preco: number,
    id: string
}