export interface Cart {
    id: string,
    quantity: number
}

export interface CartComplete {
    titulo: string,
    descricao: string,
    foto: string,
    favorito: boolean,
    preco: number,
    categoria: string
    id: string,
    quantity: number
}